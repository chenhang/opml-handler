require 'spec_helper'

describe Opml::Handler do
  it 'has a version number' do
    expect(Opml::Handler::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
